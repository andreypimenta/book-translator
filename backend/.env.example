# Tradução (LibreTranslate público; pode trocar pela sua instância)
LT_URL=https://libretranslate.com/translate
LT_API_KEY=
# OCR.space (fallback opcional – não usado por padrão)
OCRSPACE_API_KEY=
# OpenAI (apenas se quiser organização por IA – não usado por padrão)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
